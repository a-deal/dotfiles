import React, { PureComponent } from "react"

class Grid extends PureComponent {
  render() {
    return (
      <div
    )
  }
}

Grid.propTypes = {
  siteTitle: PropTypes.string,
}

Grid.defaultProps = {
  siteTitle: ``,
}

export default Grid
